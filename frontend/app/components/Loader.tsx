import { LoaderWrapper, Spinner } from "../styles/components/Loader.styles";
const Loader = () => {
  return (
    <>
      <LoaderWrapper>
        <Spinner />
      </LoaderWrapper>
    </>
  );
};

export default Loader;
